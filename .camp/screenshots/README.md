Drop the five plugin-directory screenshots here, named to match
`.camp/listing.yml` (01-….png … 05-….png) — rename freely, just keep
the listing paths in sync. png/jpg/webp accepted; registry CI resizes
to ≤1440px, strips metadata, and re-encodes to PNG (8 MB input cap).
